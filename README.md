# Framm Site

This repository contains the scaffold for the Framm website.

## Structure

- `src/app` - Next.js app routes
- `src/components` - UI components and sections
- `src/styles` - CSS layers and themes
- `public` - static site assets
